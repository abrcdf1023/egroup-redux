{
  "name": "@e-group/redux-modules",
  "version": "1.0.0",
  "private": false,
  "dependencies": {
  },
  "scripts": {
    "test": "jest",
    "clean": "rimraf build",
    "build": "yarn clean && yarn build:esm && yarn build:copy-files",
    "build:esm": "babel --config-file ../../babel.config.js ./src --out-dir ./build",
    "build:copy-files": "node ../../scripts/copy-files.js",
    "prepare": "yarn build"
  },
  "publishConfig": {
    "access": "public"
  },
  "author": "JerryP <egroup.jerrylee@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "rimraf": "^2.6.3"
  }
}
